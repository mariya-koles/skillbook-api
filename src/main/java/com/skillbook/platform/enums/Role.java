package com.skillbook.platform.enums;

/**
 * Enumeration representing user roles in the Skillbook Platform.
 */
public enum Role {
    /** Role for instructors who can create and manage courses. */
    INSTRUCTOR,
    /** Role for learners who can enroll in courses. */
    LEARNER,
    /** Role for administrators with full system access. */
    ADMIN
}
